<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="?attr/bgSurface"
    app:strokeColor="?attr/dividerColor"
    app:strokeWidth="1dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <!-- Drag handle -->
        <ImageView
            android:id="@+id/iv_drag_handle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_drag_handle"
            app:tint="?attr/textMuted"
            android:layout_marginEnd="12dp" />

        <!-- Content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Top row: ISO badge · name · restore button -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="6dp">

                <!-- Language · Script name -->
                <TextView
                    android:id="@+id/tv_script_name"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textColor="?attr/textPrimary"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <!-- ISO badge left, beside name, accent_bg -->
                <TextView
                    android:id="@+id/tv_script_code"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#FFFFFF"
                    android:textSize="10sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.06"
                    android:background="@drawable/bg_chip_active"
                    android:backgroundTint="?attr/colorPrimary"
                    android:paddingStart="7dp"
                    android:paddingEnd="7dp"
                    android:paddingTop="2dp"
                    android:paddingBottom="2dp"
                    android:layout_marginEnd="8dp" />
                    
                <!-- Restore default icon button: small, square with rounded edges, accent_bg -->
                <ImageButton
                    android:id="@+id/btn_restore_default"
                    android:layout_width="26dp"
                    android:layout_height="26dp"
                    android:layout_marginStart="8dp"
                    android:src="@drawable/ic_refresh"
                    android:background="@drawable/bg_icon_btn"
                    android:backgroundTint="?attr/accentDim"
                    app:tint="#FFFFFF"
                    android:padding="5dp"
                    android:contentDescription="Restore default"
                    android:scaleType="fitCenter" />

            </LinearLayout>

            <!-- Editable sample text -->
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/et_sample_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_input"
                android:textColor="?attr/textPrimary"
                android:textSize="13sp"
                android:padding="8dp"
                android:inputType="text|textMultiLine"
                android:maxLines="3"
                android:minLines="1" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
