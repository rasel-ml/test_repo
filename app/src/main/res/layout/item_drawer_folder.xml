<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginBottom="4dp">

    <!-- Parent folder row -->
    <LinearLayout
        android:id="@+id/row_folder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_drawer_item"
        android:padding="10dp">

        <TextView
            android:layout_width="28dp"
            android:layout_height="wrap_content"
            android:text="📁"
            android:textSize="16sp"
            android:gravity="center" />

        <TextView
            android:id="@+id/tv_folder_path"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:textColor="?attr/textSecondary"
            android:textSize="12sp"
            android:maxLines="2"
            android:ellipsize="middle" />

        <!-- Expand arrow — visible only when recursive + has sub-folders -->
        <TextView
            android:id="@+id/btn_expand"
            android:layout_width="32dp"
            android:layout_height="36dp"
            android:gravity="center"
            android:text="⌄"
            android:textSize="16sp"
            android:textColor="?attr/textMuted"
            android:visibility="gone"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <TextView
            android:id="@+id/btn_reload"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:gravity="center"
            android:text="↺"
            android:textSize="18sp"
            android:textColor="?attr/accentColor"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <TextView
            android:id="@+id/btn_remove_folder"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:gravity="center"
            android:text="✕"
            android:textSize="14sp"
            android:textColor="?attr/textMuted"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless" />

    </LinearLayout>

    <!-- Sub-folder list — collapsed by default -->
    <LinearLayout
        android:id="@+id/container_subfolders"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="4dp"
        android:paddingTop="2dp"
        android:paddingBottom="4dp"
        android:visibility="gone" />

</LinearLayout>
